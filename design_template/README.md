
  # High-fidelity dark-mode UI

  This is a code bundle for High-fidelity dark-mode UI. The original project is available at https://www.figma.com/design/4MWLEuFdaZ2y0oQRrDlA8C/High-fidelity-dark-mode-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  